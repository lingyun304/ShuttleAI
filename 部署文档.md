# 剧梭AI（Shuttle）短剧制作平台 — 部署与运行文档

本文档提供**剧梭AI（Shuttle）短剧制作平台**（MVP Phase 1 阶段）的环境准备、本地开发启动、项目构建与生产部署说明。

---

## 一、环境准备

1. **Node.js 环境**
   - 推荐 Node.js 版本：`v18.x` 或 `v20.x` 以上。
   - 常用包管理器：`npm` (v9+) / `pnpm` / `yarn`。

2. **前端与依赖说明**
   - **框架**：React 18 + Vite 5
   - **图标库**：`lucide-react`
   - **核心设计**：Vanilla CSS 动态设计系统（暗黑科技风 + 玻璃拟物特效）
   - **核心模型适配**：内置可灵 AI (Kling) 默认生成模型沙盒驱动 (`src/services/mockDriver.js`)

---

## 二、项目安装与本地启动

1. **安装依赖**
   ```bash
   cd shuttle
   npm install
   ```

2. **启动本地开发服务器**
   ```bash
   npm run dev
   ```
   - 默认访问地址：`http://localhost:3000` 或 `http://localhost:5173`
   - 具有热更新 (HMR) 支持。

---

## 三、生产构建与打包

1. **执行构建打包**
   ```bash
   npm run build
   ```
   - 构建产物将自动生成在 `./dist` 目录下（包含超轻量级 `index.html`、`assets/index.js` 及 CSS 样式文件）。

2. **本地预览打包产物**
   ```bash
   npm run preview
   ```

3. **静态托管部署**
   - 可将 `./dist` 目录直接部署至 Nginx、阿里云 OSS 静态托管、腾讯云 COS 或 Vercel / Netlify 平台的 Web 服务容器中。

---

## 四、服务端/模型配置扩展说明 (Phase 2 预留)

在 Phase 2 接通真实外网 AI 模型 API 时，可在项目根目录配置 `.env` 环境变量：

```env
# 核心视频生成模型 (默认: Kling)
VITE_DEFAULT_VIDEO_MODEL=Kling
VITE_KLING_API_KEY=your_kling_api_key_here

# LLM 剧本与审查 Agent (DeepSeek / Qwen / GPT-4o)
VITE_LLM_PROVIDER=DeepSeek
VITE_LLM_API_KEY=your_llm_api_key_here
```
